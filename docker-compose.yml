services:
  nvda-pipeline:
    build: .
    container_name: nvda_predictor
    restart: always
    shm_size: '2gb'  # Critical for Chrome/FinBERT
    volumes:
      - ./data:/app/data
      - ./models:/app/models
      - ./src/data_collection/cookies:/app/src/data_collection/cookies
      - ./src/data_collection/outputs:/app/src/data_collection/outputs
    environment:
      - TZ=UTC